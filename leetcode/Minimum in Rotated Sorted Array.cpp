class Solution 
{
public:
    int findMin(const vector<int>& nums) const
    {
        return *min_element(nums.begin(), nums.end());    
    }
};

/*



**153. 회전된 정렬 배열에서 최솟값 찾기**

길이 `n`의 오름차순 정렬된 배열이 **1번 이상 n번 이하로 회전**되었습니다.

예를 들어 `nums = [0,1,2,4,5,6,7]` 은:
- 4번 회전 시 → `[4,5,6,7,0,1,2]`
- 7번 회전 시 → `[0,1,2,4,5,6,7]`

배열 `[a[0], a[1], ..., a[n-1]]`을 1번 회전하면 `[a[n-1], a[0], a[1], ..., a[n-2]]` 가 됩니다.

**서로 다른 원소**로 이루어진 정렬 후 회전된 배열 `nums`가 주어질 때, **최솟값**을 반환하세요.

반드시 **O(log n)** 시간복잡도로 풀어야 합니다.

---

**예시 1**
- 입력: `nums = [3,4,5,1,2]`
- 출력: `1`
- 설명: 원래 배열 `[1,2,3,4,5]`를 3번 회전

**예시 2**
- 입력: `nums = [4,5,6,7,0,1,2]`
- 출력: `0`
- 설명: 원래 배열 `[0,1,2,4,5,6,7]`를 4번 회전

**예시 3**
- 입력: `nums = [11,13,15,17]`
- 출력: `11`
- 설명: 원래 배열 `[11,13,15,17]`를 4번 회전 (원래대로)

---

**제약 조건**
- `n == nums.length`
- `1 <= n <= 5000`
- `-5000 <= nums[i] <= 5000`
- 모든 원소는 **서로 다름**
- `nums`는 정렬 후 1~n번 회전된 배열

---

## 요약

회전된 정렬 배열에서 **O(log n)** 으로 최솟값 탐색 → **이진 탐색** 활용.

---

## 풀이 방향

**핵심 관찰:**
```
[4, 5, 6, 7, 0, 1, 2]
         ↑mid
mid=7 > right=2 → 최솟값은 오른쪽 절반에 있음
→ left = mid + 1

[4, 5, 6, 7, 0, 1, 2]
               ↑mid
mid=1 < right=2 → 최솟값은 왼쪽 절반에 있음 (mid 포함)
→ right = mid
```

1. `left = 0`, `right = n-1`
2. `mid = (left + right) / 2`
3. `nums[mid] > nums[right]` → 최솟값은 **오른쪽** → `left = mid + 1`
4. `nums[mid] < nums[right]` → 최솟값은 **왼쪽(mid 포함)** → `right = mid`
5. `left == right` 일 때 종료 → `nums[left]` 반환
*/
